# Leftover scratch file created by the VS Code "Code Runner" extension.
# It previously contained a bare Windows path, which is not valid Python and
# broke `python -m compileall`. Kept as an empty placeholder; safe to delete.
